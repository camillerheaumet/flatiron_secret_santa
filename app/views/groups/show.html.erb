<h1><%= @group.name %></h1>
<h3> <%= @group.budget %> </h3>

<h2>Participants: <%= @group.users.length %></h2> </h2>

<h3>Group Participants:</h3>
<ul>
  <% @group.users.each do |user| %>
  <li> <%= user.name %> </li>
  <% end %>
</ul>
<%= button_to "Add Participants", add_user_path, method: :get %><br>

<%= button_to "Add existing users",  %>
